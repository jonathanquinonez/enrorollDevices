import { StyleProp, ViewStyle } from 'react-native';

/**
 * @interface ProgressBarProps
 * @since 1.0.0
 */
export interface ProgressBarProps {
  /**
   * Position number
   * @since 1.0.0
   * @example position=2
   */
  position: number;
  /**
   * @since 1.0.0
   * @example color={'#0E79B1'}
   */
  color?: string;
}
