import { StyleProp, ViewStyle } from 'react-native';

/**
 * @interface LogoProps
 * @since 1.0.0
 */
export interface LogoProps {
    width?: number;
    style?: StyleProp<ViewStyle>;
    withOutText?: boolean;
}
